name: Schedule Script

on:
  schedule:
    - cron: '55 6 * * *'  # 每天7点执行
  # 如果您希望在推送或拉取请求时也执行，可以添加如下内容
  push:
  # pull_request:

jobs:
  run-script:
    runs-on: ubuntu-latest

    services:
      chrome:
        image: selenium/standalone-chrome:latest
        ports:
          - 4444:4444
        options: >-
          --shm-size=2g

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'  # 指定Python版本

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt  # 如果有依赖，确保您在仓库中添加了requirements.txt文件

    - name: Run the script
      run: python task.py --username 24210240085 --password U202012152lqx. --reservation-arena 张江校区网球场  --reservation-time 09:00 # 替换为您的脚本名称